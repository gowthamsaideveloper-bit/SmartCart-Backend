# # server.port=8080
# # spring.datasource.url=jdbc:mysql://localhost:3306/smartcart_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
# # spring.datasource.username=smartcart_user
# # spring.datasource.password=smartcart123
# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true
# # spring.jpa.open-in-view=false


# # Server port
# server.port=${PORT:8080}

# # Database configuration
# # Local default = MySQL
# # Online Render = PostgreSQL using environment variables
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartcart_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:smartcart_user}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:smartcart123}

# # JPA / Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.open-in-view=false

# # Avoid lazy serialization errors
# spring.jackson.serialization.fail-on-empty-beans=false


server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartcart_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:smartcart_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:smartcart123}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}

spring.jackson.serialization.fail-on-empty-beans=false